<Window x:Class="oop_lab4.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:oop_lab4"
        xmlns:vm="clr-namespace:oop_lab4.ViewModels"
        xmlns:debug="debug-mode"
        mc:Ignorable="d"
        Title="MainWindow" Height="320" Width="520"
        MinHeight="270" MinWidth="400" SizeToContent="WidthAndHeight">

    <Window.Resources>
        <Style TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
        </Style>
    </Window.Resources>

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ToolBar>
            <ToggleButton ToolTip="Добавить фигуру"
                          Command="{Binding ShowAddWindowCommand}">
                <Image Source="/Images/add.ico"/>
            </ToggleButton>
            <Separator/>
            <ToggleButton ToolTip="Удалить фигуру"
                          Command="{Binding RemoveShapeCommand}">
                <Image Source="/Images/delete.ico"/>
            </ToggleButton>
            <mc:AlternateContent>
                <mc:Choice Requires="debug">
                    <Separator/>
                    <ToggleButton ToolTip="Create Random Data"
                          Command="{Binding RandomizeShapeCommand}">
                        <Image Source="/Images/rand.ico"/>
                    </ToggleButton>
                </mc:Choice>
            </mc:AlternateContent>
            <Separator/>
            <ToggleButton ToolTip="Поиск"
                          Command="{Binding ShowSearchWindowCommand}">
                <Image Source="/Images/search.ico"/>
            </ToggleButton>
            <Separator/>
            <ToggleButton ToolTip="Сохранить в файл"
                          Command="{Binding SaveShapeCollectionCommand}">
                <Image Source="/Images/save.ico"/>
            </ToggleButton>
            <Separator/>
            <ToggleButton ToolTip="Загрузить файл"
                          Command="{Binding LoadShapeCollectionCommand}">
                <Image Source="/Images/Load.ico"/>
            </ToggleButton>
        </ToolBar>

        <GroupBox Header="Фигуры" Padding="5" Grid.Row="1">

            <StackPanel>

                <ListView ItemsSource="{Binding Shapes}"
                          SelectedItem="{Binding SelectedShape}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn
                                DisplayMemberBinding=
                                "{Binding NameOfShape}"
                                Header="Название"/>
                            <GridViewColumn
                                DisplayMemberBinding=
                                "{Binding ParametersOfShape}"
                                Header="Параметры"/>
                            <GridViewColumn
                                DisplayMemberBinding=
                                "{Binding Volume}"
                                Header="Объем"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
